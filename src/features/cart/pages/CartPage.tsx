import { Header } from "@/ui";
import { CartList } from "../components/CartList";

export const CartPage = () => {
  return (
    <div>
      <Header>Cart</Header>
      <CartList />
    </div>
  );
};
